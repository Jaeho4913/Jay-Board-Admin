<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.example.boardadmin.mapper.AdminBoardGroupMapper">

	<select id="findAll"
		resultType="com.example.boardadmin.dto.AdminBoardGroupDTO">
		SELECT
		board_group_idx AS boardGroupIdx,
		board_group_name AS
		boardGroupName,
		description,
		sort_order AS sortOrder,
		is_active AS
		active,
		created_at AS createdAt,
		updated_at AS updatedAt
		FROM
		tb_board_group
		ORDER BY sort_order ASC, board_group_idx ASC
	</select>

	<insert id="insert"
		parameterType="com.example.boardadmin.dto.AdminBoardGroupDTO">
		INSERT
		INTO tb_board_group(
		board_group_name,
		description,
		sort_order,
		is_active
		)
		VALUES(
		#{boardGroupName},
		#{description},
		#{sortOrder},
		#{active}
		)
	</insert>

	<select id="findById"
		resultType="com.example.boardadmin.dto.AdminBoardGroupDTO">
		SELECT
		board_group_idx AS boardGroupIdx,
		board_group_name AS
		boardGroupName,
		description,
		sort_order AS sortOrder,
		is_active AS
		active
		FROM tb_board_group
		WHERE board_group_idx = #{boardGroupIdx}
	</select>
	
	<update id="update">
	UPDATE tb_board_group
	SET board_group_name = #{boardGroupName},
	description = #{description},
	sort_order = #{sortOrder},
	is_active = #{active}
	WHERE board_group_idx = #{boardGroupIdx}
	</update>

</mapper>